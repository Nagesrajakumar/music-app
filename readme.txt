install requirements.txt
-> pip install -r requirements.txt

if you are running the app for the first time, you should initialize the database by running the init_db.py
-> python3 init_db.py


run the app using the following command
-> flask --app app run

in your browser open http://localhost:5000/home
